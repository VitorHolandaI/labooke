# Vite dev server port
VITE_DEV_PORT=5173

# Where the dev server forwards /api/* requests (your FastAPI host).
# In prod the frontend nginx proxies /api/ to the api container; this
# value is dev-only.
VITE_API_URL=http://127.0.0.1:8000
